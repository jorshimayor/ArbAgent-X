{
  "name": "skinbook",
  "version": "0.1.0",
  "description": "No-show booking deposits with skin in the game. Refundable, yield-bearing USDC deposits on Base.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:sepolia": "hardhat ignition deploy ./ignition/modules/SkinBook.ts --network baseSepolia",
    "seed:sepolia": "hardhat run scripts/seed-businesses.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17",
    "typescript": "^5.6.3"
  }
}
